// Package types contains the public type definitions for the Codex SDK:
// thread events, thread items, configuration options and error types.
//
// The event and item types mirror the JSONL protocol emitted by
// `codex exec --experimental-json` (see codex-rs/exec/src/exec_events.rs in
// the openai/codex repository) and the shapes used by the official
// TypeScript SDK (@openai/codex-sdk).
package types
